#include "Global.h"

int main()
{
	com = initRS232();
	usleep(5000000);
	char* temp = "This is a book. Constructs a new String by decoding the specified subarray of bytes using the specified charset. The length of the new String is a function of the charset, and hence may not be equal to the length of the subarray. ";
	send((void*)temp, STRING);
	while(1) {
	//	send((void*)temp, STRING);
	//	usleep(100000);
	//	printf("%s\n", com.data);
	}


	return 0;
}
