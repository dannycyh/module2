#include "Global.h"

int main()
{
	com = initRS232();
	char* temp = "This is a book.";
	send((void*)temp, STRING);
	send((void*)temp, STRING);
	send((void*)temp, STRING);
	send((void*)temp, STRING);
	send((void*)temp, STRING);
	send((void*)temp, STRING);
	while(1) {
	//	printf("%s\n", com.data);
	}


	return 0;
}
